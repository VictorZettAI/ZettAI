import { render, screen } from '@testing-library/react';
import { IAS } from './IAS';

describe('IAS', () => {
  it('renders correctly', () => {
    render(<IAS />);
    // Add your test assertions here
  });
});